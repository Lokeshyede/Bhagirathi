import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.bhagirathi.admin",
  appName: "Bhagirathi Admin",
  webDir: "dist",
  bundledWebRuntime: false
};

export default config;
