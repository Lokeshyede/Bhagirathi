import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.bhagirathi.tenant",
  appName: "Bhagirathi Tenant",
  webDir: "dist",
  bundledWebRuntime: false
};

export default config;
