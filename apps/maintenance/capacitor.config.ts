import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.bhagirathi.maintenance",
  appName: "Bhagirathi Maintenance",
  webDir: "dist",
  bundledWebRuntime: false
};

export default config;
